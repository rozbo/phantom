@using System.Linq
@using System.Security.Claims

@if (User.Identity.IsAuthenticated)
{
    <h2>@User.Identity.Name</h2>
    <h2>@User.Claims.FirstOrDefault(x => x.Type == ClaimTypes.Role).Value</h2>
}
else
{
    <h2>nothing here!</h2>
}